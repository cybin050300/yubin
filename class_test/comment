일반 클래스
	상속 / interface 클래스 구현
	변수 / 생성자 / 메서드 선언
	
interface 클래스
	상속(단, 다중 상속 가능)
	interface 키워드로 선언
	상수 / 메서드명만 선언 가능
	
추상 클래스 : 상속할 때만 사용 가능
	일반 클래스 + interface 클래스
	선언부에 반드시 abstract 키워드가 필요 
	변수 / 생성자 / 메서드 선언 / 상수 / 메서드명 모두 가능
	생성자를 통해 자체 객체 생성 불가!!		
	